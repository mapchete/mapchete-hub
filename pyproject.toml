[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mapchete-hub"
dynamic = ["version"]
description = "distributed mapchete processing"
readme = "README.rst"
license = "MIT"
authors = [
    { name = "Joachim Ungar", email = "joachim.ungar@eox.at" },
    { name = "Petr Sevcik", email = "petr.sevcik@eox.at" }
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.7",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Topic :: Scientific/Engineering :: GIS",
]
dependencies = [
    "aioredis>=2.0.0a1",
    "cached_property",
    "dask",
    "dask-gateway",
    "distributed",
    "fastapi==0.103.1",
    "mapchete[contours,geobuf,http,s3,vrt]>=2022.9.0",
    "mongomock",
    "pydantic<2.0.0",    
    "odmantic",
    "pymongo",
    "uvicorn",
]

[project.optional-dependencies]
complete = [
    "aioredis>=2.0.0a1",
    "cached_property",
    "dask",
    "dask-gateway",
    "distributed",
    "fastapi==0.103.1",
    "mapchete[contours,geobuf,http,s3,vrt]>=2022.9.0",
    "mapchete_xarray>=2022.5.0",
    "mongomock",
    "odmantic",
    "pydantic<2.0.0",
    "pymongo",
    "Slacker",
    "uvicorn",
]
slack = [
    "Slacker",
]
test = [
    "pytest",
    "pytest-cov",
    "pytest-env",
    "pytest-mongodb",
    "requests",
]
xarray = [
    "mapchete_xarray>=2022.5.0",
]

[project.scripts]
mhub-server = "mapchete_hub.cli:main"

[project.urls]
Homepage = "https://gitlab.eox.at/maps/mapchete_hub"

[tool.hatch.version]
path = "mapchete_hub/__init__.py"

[tool.hatch.build.targets.sdist]
include = [
    "/mapchete_hub",
]

[tool.black]
include = '\.pyi?$'
exclude = '''
/(
    \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
)/
'''
